# Исправление ошибки "[nuxt] instance unavailable"

## 🐛 Проблема

При открытии приложения в браузере появлялась ошибка:

```
Uncaught Error: [nuxt] instance unavailable
    at be (DeJyjyBh.js:4:72544)
    at br (DeJyjyBh.js:4:72616)
    at M_ (DeJyjyBh.js:4:159853)
    at DeJyjyBh.js:4:160043
```

## 🔍 Причина

В файле `app/lib/trpc.ts` tRPC клиент создавался **сразу при импорте модуля**, до инициализации Nuxt:

```typescript
// ❌ НЕПРАВИЛЬНО - вызывается сразу при импорте
export const trpc = createTRPCClientWithToken()
```

Внутри `createTRPCClientWithToken()` используется `useRuntimeConfig()`, который доступен **только после инициализации Nuxt**.

## ✅ Решение

Реализована **lazy initialization** (ленивая инициализация) с помощью Proxy:

```typescript
// ✅ ПРАВИЛЬНО - создается только при первом использовании
let _trpcClient: ReturnType<typeof createTRPCClient<AppRouter>> | null = null

export const trpc = new Proxy({} as ReturnType<typeof createTRPCClient<AppRouter>>, {
  get(target, prop) {
    if (!_trpcClient) {
      _trpcClient = createTRPCClientWithToken()
    }
    return Reflect.get(_trpcClient, prop)
  }
})
```

### Как это работает:

1. При импорте `trpc` создается только Proxy объект (легковесный)
2. При **первом обращении** к любому свойству/методу (например, `trpc.forms.getForms()`):
   - Proxy перехватывает обращение
   - Создает реальный tRPC клиент через `createTRPCClientWithToken()`
   - В этот момент Nuxt уже инициализирован, `useRuntimeConfig()` работает
3. При последующих обращениях используется уже созданный клиент

## 📝 Измененные файлы

- ✅ `applications/web-app/app/lib/trpc.ts` - добавлена lazy initialization

## 🧪 Проверка

После этого исправления:

1. Ошибка `[nuxt] instance unavailable` больше не должна появляться
2. tRPC клиент будет работать корректно
3. Все API запросы должны работать без проблем

## 🚀 Деплой

Это исправление нужно добавить к предыдущим изменениям:

```bash
# Добавить к предыдущему коммиту
git add applications/web-app/app/lib/trpc.ts
git commit -m "fix: lazy initialization for tRPC client to fix 'instance unavailable' error"
git push origin master
```

Или если еще не коммитили предыдущие изменения:

```bash
# Добавить ко всем изменениям
git add .
git commit -m "fix: use Nuxt built-in server and fix instance unavailable error"
git push origin master
```

## 📚 Дополнительная информация

### Когда возникает эта ошибка?

Ошибка `[nuxt] instance unavailable` возникает когда:
- Nuxt composables (`useRuntimeConfig`, `useRouter`, `useNuxtApp`, и т.д.) вызываются вне контекста Nuxt
- Обычно это происходит при вызове на **верхнем уровне модуля** (при импорте)

### Как избежать?

✅ **Правильно:**
```typescript
// Вызывать composables внутри функций
export function useMyComposable() {
  const config = useRuntimeConfig() // ✅ Вызывается при использовании
  return config
}
```

❌ **Неправильно:**
```typescript
// НЕ вызывать composables на верхнем уровне модуля
const config = useRuntimeConfig() // ❌ Вызывается при импорте
export function useMyComposable() {
  return config
}
```

### Альтернативное решение

Можно было бы сделать функцию вместо константы:

```typescript
// Вариант 2 - функция
export function useTrpc() {
  return createTRPCClientWithToken()
}

// Использование
const api = useApi()
const trpc = useTrpc()
const forms = await trpc.forms.getForms.query()
```

Но Proxy решение удобнее, т.к. не требует изменений в коде использования.

## ✅ Итоговый чек-лист

- [x] Исправлена ошибка "[nuxt] instance unavailable"
- [x] tRPC клиент использует lazy initialization
- [x] Все существующий код продолжает работать без изменений
- [ ] Закоммитить и запушить изменения
- [ ] Дождаться сборки в CI/CD
- [ ] Задеплоить на сервер

---

**Дата исправления:** 2025-12-07  
**Файл:** `applications/web-app/app/lib/trpc.ts`  
**Статус:** Исправлено ✅


# 🔧 Исправление проблемы с запуском frontend контейнера

## 📋 Содержание

1. [Быстрый старт](#-быстрый-старт) ⭐
2. [Описание проблемы](#-описание-проблемы)
3. [Что было исправлено](#-что-было-исправлено)
4. [Инструкции по деплою](#-инструкции-по-деплою)
5. [Документация](#-документация)

---

## ⭐ Быстрый старт

### Что нужно сделать прямо сейчас:

1. **Прочитайте:** [QUICK_DEPLOY_GUIDE.md](QUICK_DEPLOY_GUIDE.md) - пошаговая инструкция
2. **Используйте:** [DOCKER_COMPOSE_EXAMPLE.md](DOCKER_COMPOSE_EXAMPLE.md) - пример конфигурации для сервера
3. **При проблемах:** [SERVER_DEPLOYMENT_CONFIG.md](SERVER_DEPLOYMENT_CONFIG.md) - детальная документация и troubleshooting

---

## 🔍 Описание проблемы

### Симптомы:

На сервере при выполнении `docker compose up backend frontend` контейнеры запускались, но frontend сразу останавливался:

```bash
frontend-1  | /docker-entrypoint.sh: Configuration complete; ready for start up
Gracefully stopping... (press Ctrl+C again to force)
```

Backend запускался успешно, но frontend не работал.

### Причины:

1. **Неправильная архитектура:** Использовался nginx для отдачи статики, хотя Nuxt.js имеет встроенный веб-сервер (Nitro)
2. **Конфликт портов:** nginx был настроен на порт 80, но docker-compose ожидал порт 3000
3. **Отсутствие переменных окружения:** Frontend не знал как подключиться к backend API
4. **Проблемы с CORS:** Backend не был настроен на разрешение запросов с frontend домена

---

## ✅ Что было исправлено

### 1. Dockerfile (`applications/web-app/Dockerfile`)

**Было:**

```dockerfile
FROM nginx:alpine
COPY --from=builder /app/.output/public /usr/share/nginx/html
COPY nginx.conf /etc/nginx/nginx.conf
CMD ["nginx", "-g", "daemon off;"]
```

**Стало:**

```dockerfile
FROM node:24-alpine
COPY --from=builder /app/.output /app/.output
CMD ["node", ".output/server/index.mjs"]
```

**Изменения:**

- ✅ Убран nginx
- ✅ Используется встроенный Nuxt.js (Nitro) сервер
- ✅ Слушает на порту 3000 (стандартный для Nuxt)
- ✅ Поддержка SSR из коробки (если понадобится)

### 2. nuxt.config.ts

**Добавлено:**

```typescript
routeRules: process.env.BACKEND_APP_URL && process.env.NODE_ENV === "production"
  ? {
      "/api/**": {
        proxy: `${process.env.BACKEND_APP_URL}/api/**`,
        cors: true,
      },
    }
  : {},
```

**Что это дает:**

- ✅ Проксирование API запросов в production режиме
- ✅ Нет CORS проблем при использовании проксирования
- ✅ Работает одинаково в dev и production

### 3. Переменные окружения

**Нужно добавить в docker-compose.yml на сервере:**

#### Backend:

```yaml
environment:
  FRONTEND_APP_URL: "https://webapp.studsovet.kosygin-rsu.ru" # для CORS
```

#### Frontend:

```yaml
environment:
  BACKEND_APP_URL: "https://api.webapp.studsovet.kosygin-rsu.ru"
  FRONTEND_APP_URL: "https://webapp.studsovet.kosygin-rsu.ru"
  API_URL: "/api"
  HOST: "0.0.0.0"
  PORT: "3000"
  NODE_ENV: "production"
```

---

## 🚀 Инструкции по деплою

### Этап 1: На локальной машине

```bash
# 1. Убедитесь что все изменения на месте
git status

# 2. Закоммитьте изменения
git add .
git commit -m "fix: use Nuxt built-in server instead of nginx, add proper env config"

# 3. Запушьте в репозиторий
git push origin master  # или ваша ветка
```

### Этап 2: В GitLab CI/CD

Дождитесь успешной сборки новых Docker образов:

- ✅ `registry.gitlab.kosygin-rsu.ru/studsovet/studform-trpc-vue/frontend:master`
- ✅ `registry.gitlab.kosygin-rsu.ru/studsovet/studform-trpc-vue/backend:master`

### Этап 3: На сервере

```bash
# 1. Подключитесь к серверу
ssh cio@rating

# 2. Перейдите в директорию проекта
cd ~/projects/studsovet

# 3. Отредактируйте docker-compose.yml
nano docker-compose.yml
# Добавьте переменные окружения (см. DOCKER_COMPOSE_EXAMPLE.md)

# 4. Остановите контейнеры
docker compose down frontend backend

# 5. Получите новые образы
docker pull registry.gitlab.kosygin-rsu.ru/studsovet/studform-trpc-vue/frontend:master
docker pull registry.gitlab.kosygin-rsu.ru/studsovet/studform-trpc-vue/backend:master

# 6. Запустите контейнеры
docker compose up -d frontend backend

# 7. Проверьте логи
docker compose logs -f frontend backend
```

### Этап 4: Проверка

```bash
# Проверьте статус контейнеров
docker ps | grep -E "frontend|backend"

# Проверьте переменные окружения
docker exec -it studsovet-frontend-1 env | grep -E "BACKEND|HOST|PORT"
docker exec -it studsovet-backend-1 env | grep FRONTEND

# Проверьте доступность через curl
curl https://webapp.studsovet.kosygin-rsu.ru
curl https://api.webapp.studsovet.kosygin-rsu.ru/api/health
```

Откройте в браузере:

- Frontend: https://webapp.studsovet.kosygin-rsu.ru
- Проверьте DevTools → Network на наличие ошибок

---

## 📚 Документация

### Основные файлы (читайте в этом порядке):

1. **[FIX_SUMMARY.md](FIX_SUMMARY.md)**

   - Краткое резюме проблемы и решения
   - Быстрый обзор изменений

2. **[QUICK_DEPLOY_GUIDE.md](QUICK_DEPLOY_GUIDE.md)** ⭐ **НАЧНИТЕ С ЭТОГО!**

   - Пошаговая инструкция по деплою
   - Что ожидать на каждом шаге
   - Как проверить что всё работает

3. **[DOCKER_COMPOSE_EXAMPLE.md](DOCKER_COMPOSE_EXAMPLE.md)**

   - Пример конфигурации docker-compose.yml
   - Готовый код для копирования
   - Что именно нужно изменить

4. **[SERVER_DEPLOYMENT_CONFIG.md](SERVER_DEPLOYMENT_CONFIG.md)**

   - Подробная документация
   - Разные варианты конфигурации
   - Troubleshooting и частые проблемы

5. **[ENVIRONMENT_SETUP.md](ENVIRONMENT_SETUP.md)**
   - Настройка переменных окружения
   - Как работает проксирование
   - Настройка CORS

### Дополнительные файлы:

- **[ENV_URLS_GUIDE.md](ENV_URLS_GUIDE.md)** - общее руководство по настройке URL между сервисами
- **[applications/web-app/DOCKERFILE_README.md](applications/web-app/DOCKERFILE_README.md)** - документация по Dockerfile

---

## 🌐 Ваша инфраструктура

### Домены:

- **Frontend:** `https://webapp.studsovet.kosygin-rsu.ru`
- **Backend API:** `https://api.webapp.studsovet.kosygin-rsu.ru`

### Порты на сервере:

- **Frontend контейнер:** `localhost:5201` → `container:3000`
- **Backend контейнер:** `localhost:5200` → `container:3000`

### Архитектура:

```
┌──────────────────────────────────────────────────────┐
│  Внешний мир (пользователи)                          │
└────────────────┬─────────────────────────────────────┘
                 │
    ┌────────────┴──────────────┐
    │                           │
    ▼                           ▼
┌────────────────────┐   ┌──────────────────────┐
│  webapp.studsovet  │   │  api.webapp.studsovet│
│  (nginx SSL)       │   │  (nginx SSL)         │
│  порт 443          │   │  порт 443            │
└─────────┬──────────┘   └──────────┬───────────┘
          │                         │
          ▼                         ▼
    localhost:5201            localhost:5200
          │                         │
          ▼                         ▼
┌────────────────────┐   ┌──────────────────────┐
│  Frontend          │   │  Backend             │
│  Container         │───│  Container           │
│  (Nuxt server)     │   │  (tRPC server)       │
│  порт 3000         │   │  порт 3000           │
└────────────────────┘   └──────────────────────┘
```

---

## ✅ Чек-лист изменений

### В коде (✅ уже сделано):

- [x] Изменен `applications/web-app/Dockerfile` - убран nginx, добавлен Node.js
- [x] Обновлен `applications/web-app/nuxt.config.ts` - добавлены routeRules
- [x] Помечен `applications/web-app/nginx.conf` как deprecated
- [x] CORS правильно настроен в `applications/server/src/index.ts`
- [x] Создана документация

### На сервере (⚠️ нужно сделать):

- [ ] Обновить код (`git pull`)
- [ ] Дождаться сборки новых образов в CI/CD
- [ ] Отредактировать `~/projects/studsovet/docker-compose.yml`
- [ ] Добавить `FRONTEND_APP_URL` в backend сервис
- [ ] Добавить все переменные окружения в frontend сервис
- [ ] Получить новые Docker образы (`docker pull`)
- [ ] Перезапустить контейнеры (`docker compose up -d`)
- [ ] Проверить логи (`docker compose logs -f`)
- [ ] Проверить работу в браузере

---

## 🆘 Получение помощи

### Если что-то не работает:

1. **Проверьте логи:**

   ```bash
   docker compose logs -f frontend backend
   ```

2. **Проверьте переменные окружения:**

   ```bash
   docker exec -it studsovet-frontend-1 env | grep -E "BACKEND|HOST|PORT"
   ```

3. **Смотрите раздел Troubleshooting:**

   - [SERVER_DEPLOYMENT_CONFIG.md](SERVER_DEPLOYMENT_CONFIG.md) - раздел "Частые проблемы и решения"

4. **Проверьте что образы обновились:**
   ```bash
   docker images | grep studform-trpc-vue
   # Проверьте дату создания - должна быть свежая
   ```

---

## 🎉 Что дальше?

После успешного деплоя:

1. ✅ Frontend должен быть доступен по адресу `https://webapp.studsovet.kosygin-rsu.ru`
2. ✅ API должен отвечать на `https://api.webapp.studsovet.kosygin-rsu.ru/api/health`
3. ✅ Telegram Mini App должен работать через бота
4. ✅ Все API запросы должны работать без ошибок CORS

**Готово!** Ваше приложение должно работать корректно. 🚀

---

## 📝 Заметки

- Файл `nginx.conf` в директории `applications/web-app/` больше не используется, но оставлен для истории
- Все настройки теперь в `nuxt.config.ts` и переменных окружения
- Если в будущем понадобится SSL внутри контейнера, можно будет добавить nginx обратно, но сейчас это не нужно

---

**Дата создания:** 2025-12-07  
**Версия:** 1.0  
**Статус:** Готово к деплою ✅

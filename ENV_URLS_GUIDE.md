# 📋 Руководство по настройке URL в .env файлах

## 🎯 Общая концепция

Каждый сервис должен знать URL других сервисов, с которыми он взаимодействует:

- **Frontend (web-app)** должен знать URL **Backend** для отправки API запросов
- **Backend (server)** должен знать URL **Frontend** для настройки CORS
- **Telegram Bot** должен знать URL **Frontend** для кнопки Mini App

---

## 📁 Структура .env файлов

### Вариант 1: Один общий .env в корне проекта (рекомендуется для локальной разработки)

Если вы используете один `.env` файл в корне проекта, все сервисы будут его читать:

```
web-app-ts/
├── .env                    ← Один файл для всех
├── env.example
└── applications/
    ├── server/
    ├── web-app/
    └── telegram-bot/
```

### Вариант 2: Отдельные .env в каждой директории (для продакшена)

Каждый сервис может иметь свой `.env` файл:

```
web-app-ts/
├── env.example
└── applications/
    ├── server/
    │   └── .env            ← Только для server
    ├── web-app/
    │   └── .env            ← Только для web-app
    └── telegram-bot/
        └── .env            ← Только для telegram-bot
```

---

## 🔧 Настройка для локальной разработки

### Корневой `.env` (или `applications/server/.env`)

```env
# ============================================
# TELEGRAM BOT CONFIGURATION
# ============================================
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
WEBHOOK_URL=http://localhost:3001/webhook
MINI_APP_URL=http://localhost:3000

# ============================================
# APPLICATION URLs (для взаимодействия между сервисами)
# ============================================
# URL фронтенда - куда пользователи заходят через браузер/Telegram
FRONTEND_APP_URL=http://localhost:3000

# URL бэкенда - где работает API сервер
BACKEND_APP_URL=http://localhost:3100

# Путь к API (обычно /api, не меняйте если не уверены)
API_URL=/api

# ============================================
# DATABASE CONFIGURATION
# ============================================
DATABASE_URL=postgresql://myuser:mypassword@localhost:5432/mydb
# Или отдельно:
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=studsovet

# ============================================
# JWT CONFIGURATION
# ============================================
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=3100
NODE_ENV=development
```

### Объяснение переменных:

#### `FRONTEND_APP_URL`

- **Где используется:**
  - В **server** (для CORS) - указывает, откуда разрешены запросы
  - В **telegram-bot** (для кнопки Mini App) - куда ведет кнопка
  - В **web-app** (runtime config) - для внутренних нужд
- **Что указывать:** URL, где работает фронтенд
- **Локально:** `http://localhost:3000`
- **На проде:** `https://your-domain.com`

#### `BACKEND_APP_URL`

- **Где используется:**
  - В **web-app** (devProxy) - куда проксировать API запросы в режиме разработки
- **Что указывать:** Базовый URL бэкенда (БЕЗ `/api` в конце!)
- **Локально:** `http://localhost:3100`
- **На проде:** `https://api.your-domain.com` или `https://your-domain.com`

#### `API_URL`

- **Где используется:**
  - В **web-app** (runtime config) - путь к API для tRPC клиента
- **Что указывать:** Путь к API (обычно `/api`)
- **Локально:** `/api`
- **На проде:** `/api` (если API на том же домене) или полный URL если на другом домене

---

## 🚀 Настройка для продакшена (на удаленном сервере)

### Пример для продакшена:

```env
# ============================================
# TELEGRAM BOT CONFIGURATION
# ============================================
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
WEBHOOK_URL=https://your-domain.com/webhook
MINI_APP_URL=https://your-domain.com

# ============================================
# APPLICATION URLs
# ============================================
# Публичный URL вашего фронтенда
FRONTEND_APP_URL=https://your-domain.com

# Публичный URL вашего бэкенда (если на другом домене)
# Или тот же домен, если nginx проксирует запросы
BACKEND_APP_URL=https://your-domain.com
# Или если бэкенд на отдельном поддомене:
# BACKEND_APP_URL=https://api.your-domain.com

# Путь к API
API_URL=/api

# ============================================
# DATABASE CONFIGURATION
# ============================================
DATABASE_URL=postgresql://user:password@db-host:5432/dbname

# ============================================
# JWT CONFIGURATION
# ============================================
JWT_SECRET=your_very_secure_production_secret_key_here

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=3000
NODE_ENV=production
```

---

## 🔄 Как работают URL между сервисами

### Схема взаимодействия:

```
┌─────────────────┐
│  Telegram Bot   │
│                 │
│  Использует:    │
│  FRONTEND_APP_URL│
│  (для кнопки)   │
└────────┬────────┘
         │
         │ Открывает Mini App
         ▼
┌─────────────────┐         ┌─────────────────┐
│   Web App       │────────▶│   Backend       │
│   (Frontend)    │         │   (Server)      │
│                 │         │                 │
│  Использует:    │         │  Использует:    │
│  BACKEND_APP_URL│         │  FRONTEND_APP_URL│
│  (для API)      │         │  (для CORS)     │
│  API_URL        │         │                 │
└─────────────────┘         └─────────────────┘
```

### Детальное объяснение:

#### 1. **Web App (Frontend) → Backend (Server)**

**В `applications/web-app/.env` или корневом `.env`:**

```env
BACKEND_APP_URL=http://localhost:3100  # Локально
# или
BACKEND_APP_URL=https://your-domain.com  # На проде

API_URL=/api
```

**Как используется:**

- `BACKEND_APP_URL` - используется в `nuxt.config.ts` для devProxy (только в режиме разработки)
- `API_URL` - используется в `app/lib/trpc.ts` для формирования URL к tRPC endpoint

**Важно:**

- В режиме разработки Nuxt проксирует `/api/*` на `BACKEND_APP_URL/api/*`
- На проде (через nginx) проксирование делает nginx, поэтому `BACKEND_APP_URL` может быть тем же доменом

#### 2. **Backend (Server) → Frontend (для CORS)**

**В `applications/server/.env` или корневом `.env`:**

```env
FRONTEND_APP_URL=http://localhost:3000  # Локально
# или
FRONTEND_APP_URL=https://your-domain.com  # На проде
```

**Как используется:**

- В `applications/server/src/index.ts` для настройки CORS
- Backend разрешает запросы только с этого URL

**Важно:**

- Backend должен знать URL фронтенда, чтобы разрешить CORS запросы
- Если не указать, CORS разрешит все (только для разработки!)

#### 3. **Telegram Bot → Frontend (для кнопки Mini App)**

**В `applications/telegram-bot/.env` или корневом `.env`:**

```env
FRONTEND_APP_URL=http://localhost:3000  # Локально
# или
FRONTEND_APP_URL=https://your-domain.com  # На проде

# Альтернативно можно использовать:
MINI_APP_URL=https://your-domain.com
```

**Как используется:**

- В `applications/telegram-bot/src/index.ts` для создания кнопки Mini App
- Бот использует `FRONTEND_APP_URL` или `MINI_APP_URL` (приоритет у `MINI_APP_URL`)

---

## ✅ Чек-лист настройки

### Для локальной разработки:

- [ ] `FRONTEND_APP_URL=http://localhost:3000` (в корневом `.env`)
- [ ] `BACKEND_APP_URL=http://localhost:3100` (в корневом `.env`)
- [ ] `API_URL=/api` (в корневом `.env`)
- [ ] Backend запущен на порту 3100
- [ ] Frontend запущен на порту 3000

### Для продакшена:

- [ ] `FRONTEND_APP_URL=https://your-domain.com` (публичный URL фронтенда)
- [ ] `BACKEND_APP_URL=https://your-domain.com` (если nginx проксирует) или отдельный домен
- [ ] `API_URL=/api` (путь к API)
- [ ] CORS на бэкенде настроен на `FRONTEND_APP_URL`
- [ ] Nginx правильно проксирует запросы

---

## 🐛 Частые ошибки

### ❌ Ошибка 1: Указан `/api` в `BACKEND_APP_URL`

```env
# НЕПРАВИЛЬНО:
BACKEND_APP_URL=http://localhost:3100/api

# ПРАВИЛЬНО:
BACKEND_APP_URL=http://localhost:3100
```

**Почему:** В коде уже добавляется `/api` к `BACKEND_APP_URL` для devProxy

### ❌ Ошибка 2: Разные URL в разных .env файлах

```env
# В server/.env:
FRONTEND_APP_URL=http://localhost:3000

# В web-app/.env:
FRONTEND_APP_URL=http://localhost:3001  # ОШИБКА!
```

**Почему:** URL должны совпадать, иначе CORS не пропустит запросы

### ❌ Ошибка 3: Использование `localhost` на проде

```env
# НЕПРАВИЛЬНО для продакшена:
FRONTEND_APP_URL=http://localhost:3000

# ПРАВИЛЬНО для продакшена:
FRONTEND_APP_URL=https://your-domain.com
```

---

## 📝 Примеры для разных сценариев

### Сценарий 1: Локальная разработка (все на localhost)

```env
FRONTEND_APP_URL=http://localhost:3000
BACKEND_APP_URL=http://localhost:3100
API_URL=/api
```

### Сценарий 2: Продакшен (все на одном домене, nginx проксирует)

```env
FRONTEND_APP_URL=https://myapp.com
BACKEND_APP_URL=https://myapp.com
API_URL=/api
```

### Сценарий 3: Продакшен (бэкенд на отдельном поддомене)

```env
FRONTEND_APP_URL=https://myapp.com
BACKEND_APP_URL=https://api.myapp.com
API_URL=/api
```

---

## 🔍 Как проверить, что все настроено правильно

1. **Проверьте CORS:**

   - Откройте фронтенд в браузере
   - Откройте DevTools → Network
   - Сделайте запрос к API
   - Если видите ошибку CORS, проверьте `FRONTEND_APP_URL` в server/.env

2. **Проверьте API запросы:**

   - Откройте фронтенд
   - Проверьте в Network, куда идут запросы
   - Должны идти на правильный `BACKEND_APP_URL`

3. **Проверьте Telegram Bot:**
   - Отправьте `/start` боту
   - Нажмите на кнопку Mini App
   - Должен открыться правильный `FRONTEND_APP_URL`








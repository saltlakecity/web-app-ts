# Telegram Bot

Telegram бот для студенческого совета с интеграцией Mini App.

## 🚀 Технологии

- **Grammy** - современный Telegram Bot Framework
- **TypeScript** - типизация
- **Polling** - получение обновлений от Telegram

## 📁 Структура проекта

```
telegram-bot/
├── src/
│   └── index.ts          # Основной файл бота
├── dist/                 # Скомпилированные файлы
├── package.json
├── tsconfig.json
├── .env.example          # Пример переменных окружения
├── Dockerfile            # Docker конфигурация
└── README.md
```

## 🤖 Возможности

- ✅ Команда `/start` - приветствие и информация о боте
- ✅ Команда `/help` - справка по использованию
- ✅ Кнопка для открытия Mini App
- ✅ Polling режим для получения обновлений
- ✅ Graceful shutdown (корректное завершение)
- ✅ Обработка ошибок
- ✅ Логирование действий

## 🔧 Установка и запуск

### Требования

- Node.js >= 18
- pnpm
- Telegram бот токен от [@BotFather](https://t.me/botfather)

### 1. Создание бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите название бота (например: "Студсовет Бот")
   - Введите username бота (должен заканчиваться на `bot`, например: `studsovet_bot`)
4. Сохраните полученный токен (выглядит как `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### 2. Настройка Mini App

1. Создайте Mini App через [@BotFather](https://t.me/botfather):
   ```
   /newapp
   ```
2. Выберите вашего бота из списка
3. Укажите название приложения
4. Укажите описание (короткое и полное)
5. Загрузите иконку приложения:
   - Формат: PNG или JPG
   - Размер: минимум 640x360px
   - Соотношение сторон: 16:9
6. Загрузите GIF/видео демонстрацию (опционально)
7. Укажите URL вашего Mini App (например: `https://your-app.com`)
8. Выберите `/setmenubutton` чтобы добавить кнопку в меню бота

### 3. Установка зависимостей

```bash
pnpm install
```

### 4. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните переменные:

```env
# Токен бота от @BotFather
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# URL вашего Mini App
MINI_APP_URL=https://your-app.com

# Опционально: режим разработки
NODE_ENV=development
```

### 5. Запуск

```bash
# Режим разработки (с hot reload)
pnpm dev

# Сборка
pnpm build

# Production запуск
pnpm start
```

## 📱 Команды бота

### /start

Приветственное сообщение с кнопкой для открытия Mini App.

**Ответ:**
```
👋 Привет! Я бот студенческого совета.

Нажми на кнопку ниже, чтобы открыть приложение.

[Открыть приложение 📱]
```

### /help

Справка по использованию бота.

**Ответ:**
```
📚 Доступные команды:

/start - Начать работу с ботом
/help - Показать эту справку

Используйте кнопку "Открыть приложение" для доступа к формам.
```

## 🔄 Polling vs Webhook

Проект использует **Polling режим**:

### Преимущества Polling:

- ✅ Не требует публичного HTTPS URL
- ✅ Проще в настройке и отладке
- ✅ Работает на localhost
- ✅ Не требует настройки SSL сертификатов
- ✅ Автоматическое переподключение

### Недостатки Polling:

- ❌ Немного больше нагрузки на сервер
- ❌ Небольшая задержка в получении обновлений (обычно < 1 сек)

### Переключение на Webhook (для production):

Если вам нужен webhook режим:

```typescript
// Вместо bot.start()
await bot.api.setWebhook('https://your-domain.com/webhook', {
  drop_pending_updates: true
})

app.use(webhookCallback(bot, 'express'))
```

## 🛡️ Обработка ошибок

Бот обрабатывает следующие типы ошибок:

### Ошибки команд

```typescript
bot.catch((err) => {
  console.error('Ошибка при обработке команды:', err)
})
```

### Graceful shutdown

При получении сигналов `SIGINT` или `SIGTERM` бот:

1. Прекращает получать новые обновления
2. Завершает обработку текущих запросов
3. Закрывает соединение с Telegram API
4. Корректно завершает процесс

```typescript
process.once('SIGINT', () => bot.stop())
process.once('SIGTERM', () => bot.stop())
```

## 📊 Логирование

Бот логирует следующие события:

- ✅ Запуск бота
- ✅ Обработка команд `/start` и `/help`
- ✅ Ошибки при обработке команд
- ✅ Завершение работы

Пример логов:

```
🤖 Бот запущен и готов к работе!
👤 Пользователь @username выполнил команду /start
❌ Ошибка при обработке команды: [детали ошибки]
🛑 Бот остановлен
```

## 🐳 Docker

### Сборка образа

```bash
docker build -t studsovet-telegram-bot .
```

### Запуск контейнера

```bash
# С переменными окружения из файла
docker run -d --name telegram-bot --env-file .env studsovet-telegram-bot

# Или с явным указанием переменных
docker run -d --name telegram-bot \
  -e BOT_TOKEN=your-token \
  -e MINI_APP_URL=your-url \
  studsovet-telegram-bot
```

### Просмотр логов

```bash
docker logs -f telegram-bot
```

### Остановка и удаление

```bash
docker stop telegram-bot
docker rm telegram-bot
```

## 🚀 Production

### Рекомендации

1. **Переменные окружения**
   - Используйте безопасное хранение секретов (например, Docker Secrets, Kubernetes Secrets)
   - Не коммитьте `.env` файл в репозиторий

2. **Мониторинг**
   - Настройте логирование в файл или систему сбора логов
   - Используйте мониторинг uptime
   - Настройте алерты на ошибки

3. **Автоперезапуск**
   ```bash
   # С использованием pm2
   npm install -g pm2
   pm2 start dist/index.js --name telegram-bot
   pm2 startup
   pm2 save
   
   # С использованием systemd
   # Создайте /etc/systemd/system/telegram-bot.service
   ```

4. **Rate Limiting**
   - Grammy автоматически обрабатывает rate limits от Telegram API
   - Используйте `bot.api.config.use(autoRetry())` для автоматических повторов

### Развертывание

#### VPS/Dedicated сервер

```bash
# Клонирование репозитория
git clone <repo-url>
cd telegram-bot

# Установка зависимостей
pnpm install

# Настройка переменных окружения
cp .env.example .env
nano .env

# Сборка
pnpm build

# Запуск с pm2
pm2 start dist/index.js --name telegram-bot
pm2 save
pm2 startup
```

#### Docker Compose

```yaml
# docker-compose.yml
version: '3.8'
services:
  telegram-bot:
    build: ./applications/telegram-bot
    env_file:
      - ./applications/telegram-bot/.env
    restart: unless-stopped
```

#### Kubernetes

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: telegram-bot
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: telegram-bot
        image: studsovet-telegram-bot:latest
        env:
        - name: BOT_TOKEN
          valueFrom:
            secretKeyRef:
              name: bot-secrets
              key: token
```

## 🔒 Безопасность

### Защита токена

- ✅ Токен хранится в переменных окружения
- ✅ Файл `.env` добавлен в `.gitignore`
- ✅ Никогда не логируйте токен в консоль

### Валидация пользователей

Можно добавить проверку пользователей:

```typescript
bot.use(async (ctx, next) => {
  const userId = ctx.from?.id
  const allowedUsers = [123456, 789012] // ID разрешенных пользователей
  
  if (userId && allowedUsers.includes(userId)) {
    await next()
  } else {
    await ctx.reply('У вас нет доступа к этому боту')
  }
})
```

### Проверка администраторов

```typescript
bot.command('admin', async (ctx) => {
  const chatMember = await ctx.api.getChatMember(
    ctx.chat!.id,
    ctx.from!.id
  )
  
  if (['creator', 'administrator'].includes(chatMember.status)) {
    await ctx.reply('Вы администратор!')
  }
})
```

## 📝 Разработка

### Добавление новой команды

```typescript
bot.command('mycommand', async (ctx) => {
  await ctx.reply('Ответ на команду')
})
```

### Добавление обработчика текста

```typescript
bot.on('message:text', async (ctx) => {
  const text = ctx.message.text
  await ctx.reply(`Вы написали: ${text}`)
})
```

### Добавление inline кнопок

```typescript
import { InlineKeyboard } from 'grammy'

bot.command('buttons', async (ctx) => {
  const keyboard = new InlineKeyboard()
    .text('Кнопка 1', 'btn1')
    .text('Кнопка 2', 'btn2')
  
  await ctx.reply('Выберите кнопку:', {
    reply_markup: keyboard
  })
})

bot.callbackQuery('btn1', async (ctx) => {
  await ctx.answerCallbackQuery('Вы нажали кнопку 1')
})
```

## 🧪 Тестирование

```bash
# TODO: Добавить тесты
pnpm test
```

## 📚 Полезные ссылки

- [Grammy документация](https://grammy.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [@BotFather](https://t.me/botfather) - создание и настройка ботов

## 📄 Лицензия

[Укажите лицензию]
